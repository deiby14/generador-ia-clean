/**
 * @fileoverview PostCSS Configuration
 * @description PostCSS plugins configuration for Tailwind CSS processing
 * @author Generador IA Team
 * @version 1.0.0
 */

export default {
  plugins: {
    // Tailwind CSS plugin for utility class generation
    tailwindcss: {},
    
    // Autoprefixer for cross-browser compatibility
    autoprefixer: {},
  },
};
